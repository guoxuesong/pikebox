#define STATE_OPEN 0
#define STATE_CLOSING 1
#define STATE_CLOSED 2
#define STATE_DELETING 3

#define LOCALE(X,Y) string_to_utf8(Locale.translate("bityuan", BITYUAND->get_lang(), (X), (Y)))
#define R(X) replace(X,(["BTC":upper_case(coin),"PH":D->unit]))

#define BUTTONGAP 16
